[user]
  name = Devin Weaver
  email = suki@tritarget.org
  signingkey = DE767C8A

[alias]
  s = status -sb
  l = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %Cblue<%aN>%Creset%C(yellow)%d%Creset' --abbrev-commit --date=relative
  sl = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %Cblue<%aN>%Creset%C(yellow)%d%Creset' --abbrev-commit --date=relative -n 10
  ci = commit -v
  co = checkout
  st = status
  sb = show-branch
  diffc = diff --cached
  diffs = diff --stat
  shows = show --stat
  stats = shortlog --summary
  patch = format-patch
  rawdiff = diff --no-ext-diff -w
  hide = update-index --assume-unchanged
  unhide = update-index --no-assume-unchanged
  hidden = !git ls-files -v | grep ^[a-z]
  ap = add -p
  cim = commit -m
  serve = daemon --reuseaddr --verbose --base-path=. --export-all ./.git
  pr = !hub pull-request
  prdev = !hub pull-request -b develop
  pushtags = push --follow-tags
  pushorigin = push -u origin HEAD
  amend = commit -v --amend
  fixup = commit --fixup
  squash = commit --squash
  prune = remote prune
  pruneo = remote prune origin
  pruneall = !git remote prune origin | tee /dev/stderr | grep pruned | sed -e 's+^.*origin/+git branch -d +' | sh
  ctags = !.git/hooks/ctags
  this-branch = rev-parse --abbrev-ref HEAD

[core]
  ignorecase = false
  pager = less -EX
  excludesfile = ~/.gitignore_global
  autocrlf = false
  whitespace = cr-at-eol

[apply]
  whitespace = fix

[push]
  default = simple

[rebase]
  autosquash = true

[merge]
  ; conflictstyle = diff3
  conflictstyle = merge
  tool = fugitive

[rerere]
  enabled = true

[diff]
  tool = vimdiff

[mergetool "diffconflicts"]
  cmd = diffconflicts vim $BASE $LOCAL $REMOTE $MERGED
  trustExitCode = false
  keepBackup = false

[mergetool "fugitive"]
  cmd = vim "+Gdiff" $MERGED

[color]
  ui = auto
  interactive = auto
  status = auto
  branch = auto

[github]
  user = sukima

[gui]
  diffopts = --patience

[init]
  templatedir = ~/.git_template
